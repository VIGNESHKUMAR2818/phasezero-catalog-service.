# phasezero-catalog-service.
This project is a Spring Boot backend microservice that provides a simple product catalog service for an inventory system. It exposes REST APIs so clients can manage and query product data in a structured, consistent way.
